@using System.Drawing;
@using System.Web.UI.WebControls;
@{
    var grid = Html.DevExpress().GridView(settings =>
    {
        settings.Name = "gvOrganizations";
        settings.CallbackRouteValues = new { Controller = "Organization", Action = "OrganizationGridPartial" };
        settings.Width = Unit.Percentage(100);
        settings.SettingsPager.Visible = true;
        settings.SettingsPager.PageSize = 10;

        settings.KeyFieldName = "OrganizationID";
        settings.Columns.Add("OrgCode", "Organization Code");
        settings.Columns.Add("OrgTitle", "Organization Title");
        settings.Columns.Add("OrgGroup", "Organization Group");
        settings.Columns.Add("OrgStatus", "Status");
        settings.Columns.Add(column =>
        {
            column.SetDataItemTemplateContent(c =>
            {
                ViewContext.Writer.Write(
                    Html.ActionLink("Edit", "Edit", new { Id = DataBinder.Eval(c.DataItem, "OrganizationID") },
                     new
    {
        style = "background: url('" + Url.Content("~/Images/phpQCqtZ2AM.jpg") + "') no-repeat center right; display:block; height:15px;width:45px;"
    })
    
                );
            });
            column.Settings.AllowDragDrop = DefaultBoolean.False;
            column.Settings.AllowSort = DefaultBoolean.False;
            column.Width = 40;
        });
        settings.Columns.Add(column =>
        {
            column.SetDataItemTemplateContent(c =>
            {
                ViewContext.Writer.Write(
                    Html.ActionLink("Remove", "Delete", new { Id = DataBinder.Eval(c.DataItem, "OrganizationID") },
                        new { onclick = "return confirm('Do you really want to delete this record?')" ,style = "background: url('" + Url.Content("~/Images/phpY5HbjYAM.jpg") + "') no-repeat center right; display:block; height:15px;width:65px;"  })
                );
            });
            column.Settings.AllowDragDrop = DefaultBoolean.False;
            column.Settings.AllowSort = DefaultBoolean.False;
            column.Width = 60;
        });
    }).Bind(Model).GetHtml();
}